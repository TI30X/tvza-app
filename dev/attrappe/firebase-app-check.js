/* Attrappe — siehe firebase-firestore.js. App Check hat im Attrappen-Modus nichts zu prüfen. */
export class ReCaptchaEnterpriseProvider { constructor(key) { this.key = key; } }
export const initializeAppCheck = () => ({});
